
var assert = function (cond, message) {
    if (!cond) {
        console.warn("ERROR: " + message)
        process.exit(-1)
    }
}

function print_as_column(list) {
    list.forEach(function(x) {
. @{x} 
    });
}

function capitalize(word) {
    return word.charAt(0).toUpperCase() + word.slice(1)
}
